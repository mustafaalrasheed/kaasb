# Kaasb Load Test Requirements
# Install: pip install -r load-tests/requirements.txt

# Locust load testing framework
locust==2.32.2

# HTTP client for seeder + data generator
httpx==0.28.1

# Data analysis
pandas==2.2.3
tabulate==0.9.0

# Progress bars for seeder
tqdm==4.67.1

# k6 is a separate binary — install from https://k6.io/docs/get-started/installation/
# Linux/Mac: brew install k6  OR  sudo apt install k6
# Windows:   choco install k6
