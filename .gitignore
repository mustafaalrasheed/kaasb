# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/

# === Virtual Environment ===
venv/
.venv/
env/

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
# Word / Excel / PowerPoint owner-lock files (created when the doc is open)
~$*

# === Environment Variables — NEVER commit real secrets ===
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.staging
# Templates (CHANGE_ME values only) are tracked:
!.env.production.example
!.env.staging.example
!backend/.env.example

# === Node.js ===
node_modules/
.npm
.next/
out/
.turbo/
*.tsbuildinfo

# === Logs ===
*.log
logs/

# === Database ===
*.db
*.sqlite3
backups/*.sql
backups/*.sql.gz

# === Uploads (user content) ===
uploads/*
!uploads/.gitkeep

# === Docker volumes ===
postgres_data/
redis_data/

# === SSL Certificates ===
ssl/
*.pem
*.key
*.crt

# === Coverage & Testing ===
htmlcov/
.coverage
.coverage.*
coverage.xml
coverage/
.pytest_cache/
.mypy_cache/
.ruff_cache/
junit*.xml

# === Playwright ===
frontend/test-results/
frontend/playwright-report/
frontend/playwright/.cache/
frontend/playwright-results.xml

# === Secrets detection baseline ===
.secrets.baseline

# === Alembic (contains DB URL — use .env instead) ===
backend/alembic.ini

# === OS ===
.DS_Store
Desktop.ini

# === Claude Code ===
.claude/
