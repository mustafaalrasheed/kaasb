# === Core Framework ===
# FastAPI 0.120+ allows starlette 0.49.x (which has the CVE-2025-62727 fix)
fastapi==0.120.0
starlette==0.49.1
uvicorn[standard]==0.34.0
gunicorn==22.0.0
python-multipart==0.0.26

# === Database ===
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
greenlet==3.1.1

# === Authentication ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# === Validation & Settings ===
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# === Redis & Caching ===
redis[hiredis]==5.2.1

# === File Handling ===
python-magic==0.4.27
Pillow==12.2.0

# === HTTP Client ===
httpx==0.28.1
resend==2.7.0
jinja2==3.1.6
twilio==9.4.5          # SMS OTP delivery (production); optional — email used when not configured

# === Payment Providers ===
# wise-api handled via httpx REST calls
# stripe removed — Qi Card only (migration b2c3d4e5f6a7)

# === Monitoring & Observability ===
prometheus-fastapi-instrumentator==7.0.0  # FastAPI /metrics endpoint + HTTP metrics
prometheus-client==0.20.0                 # Custom Prometheus counters/gauges/histograms
sentry-sdk[fastapi]==2.19.2               # Error tracking + performance tracing

# === Security ===
# bleach for input sanitization (optional, we use custom)
# slowapi for production rate limiting (optional, we use custom in-memory)

# === Testing ===
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
requests==2.33.0
aiosqlite==0.20.0

# === Dev Tools ===
black==24.10.0
ruff==0.11.2
mypy==1.13.0
