# === Core Framework ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==21.2.0
python-multipart==0.0.18

# === Database ===
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
greenlet==3.1.1

# === Authentication ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# === Validation & Settings ===
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# === Redis & Caching ===
redis[hiredis]==5.2.1

# === File Handling ===
python-magic==0.4.27
Pillow==11.0.0

# === HTTP Client ===
httpx==0.28.1
resend==2.7.0
jinja2==3.1.4

# === Payment Providers ===
stripe==7.12.0
# wise-api handled via httpx REST calls

# === Monitoring & Observability ===
prometheus-fastapi-instrumentator==7.0.0  # FastAPI /metrics endpoint + HTTP metrics
prometheus-client==0.20.0                 # Custom Prometheus counters/gauges/histograms
sentry-sdk[fastapi]==2.19.2               # Error tracking + performance tracing

# === Security ===
# bleach for input sanitization (optional, we use custom)
# slowapi for production rate limiting (optional, we use custom in-memory)

# === Testing ===
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
requests==2.32.3

# === Dev Tools ===
black==24.10.0
ruff==0.11.2
mypy==1.13.0
